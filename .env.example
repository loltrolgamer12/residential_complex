# =========================================
# 🏢 Sistema Conjuntos Residenciales - Variables de Entorno
# Archivo de configuración para desarrollo
# =========================================

# IMPORTANTE: Copie este archivo como .env y configure sus valores
# cp .env.example .env

# =========================================
# CONFIGURACIÓN DEL SERVIDOR
# =========================================
PORT=3000
NODE_ENV=development

# =========================================
# BASE DE DATOS POSTGRESQL
# =========================================
DB_HOST=localhost
DB_PORT=5432
DB_NAME=residential_complex
DB_USER=postgres
DB_PASSWORD=tu_password_aqui

# Para conexión con SSL en producción
# DB_SSL=true

# =========================================
# AUTENTICACIÓN JWT
# =========================================
# GENERAR CON: openssl rand -base64 32
JWT_SECRET=tu_jwt_secret_muy_seguro_aqui_32_caracteres_minimo
JWT_EXPIRES_IN=24h

# =========================================
# RATE LIMITING Y SEGURIDAD
# =========================================
# Ventana de tiempo en milisegundos (15 minutos por defecto)
RATE_LIMIT_WINDOW_MS=900000
# Máximo de requests por ventana de tiempo
RATE_LIMIT_MAX_REQUESTS=100

# =========================================
# CORS CONFIGURACIÓN
# =========================================
# Origen permitido para CORS (en producción usar dominio específico)
CORS_ORIGIN=http://localhost:3000
# Múltiples orígenes separados por coma
# CORS_ORIGIN=http://localhost:3000,https://midominio.com

# =========================================
# LOGGING CONFIGURACIÓN
# =========================================
# Niveles: error, warn, info, debug
LOG_LEVEL=debug

# Directorio para logs
LOG_DIR=logs

# =========================================
# CONFIGURACIÓN DE BACKUP
# =========================================
BACKUP_ENABLED=true
# Intervalo en milisegundos (24 horas por defecto)
BACKUP_INTERVAL=86400000
# Días de retención de backups
BACKUP_RETENTION_DAYS=7
# Directorio de backups
BACKUP_DIR=database/backups

# =========================================
# CONFIGURACIÓN DE TESTING
# =========================================
TEST_DB_NAME=residential_complex_test
TEST_PORT=3001

# =========================================
# CONFIGURACIÓN DE PRODUCCIÓN
# =========================================
# Configuraciones adicionales para producción

# PM2 Configuration
PM2_INSTANCES=max
PM2_MAX_MEMORY_RESTART=1G

# Email (para notificaciones futuras)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# File Upload (para futuras funcionalidades)
# MAX_FILE_SIZE=5MB
# UPLOAD_DIR=uploads

# Redis (para cache futuro)
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=

# =========================================
# CONFIGURACIÓN DE DESARROLLO
# =========================================
# Variables específicas para desarrollo

# Debug mode
DEBUG=residential:*

# Hot reload
NODEMON_IGNORE=logs/*,uploads/*,database/backups/*

# =========================================
# NOTAS DE SEGURIDAD
# =========================================
# 1. NUNCA commitee este archivo con credenciales reales
# 2. Use contraseñas fuertes en producción
# 3. Configure JWT_SECRET único por instalación
# 4. Limite CORS_ORIGIN en producción
# 5. Use HTTPS en producción
# 6. Configure firewall para PostgreSQL
# 7. Use SSL para conexión a base de datos en producción
